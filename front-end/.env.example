# =============================================================================
# PRODUCTION CONFIGURATION
# =============================================================================
# Using relative path /api ensures frontend works with both HTTP and HTTPS
# This relies on nginx reverse proxy to route /api/* to backend container
# DO NOT use absolute URLs like http://46.250.244.245/api (causes mixed content errors)

VITE_API_URL=/api

# =============================================================================
# DEVELOPMENT NOTE
# =============================================================================
# For local development outside Docker, you can temporarily use:
# VITE_API_URL=http://localhost:8000/api
# But remember to change it back to /api before building for production

# =============================================================================
# IMPORTANT
# =============================================================================
# Copy this file to .env before building:
# cp .env.example .env
