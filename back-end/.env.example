# =============================================================================
# Nexora001 Environment Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# NEVER commit .env to version control!

# -----------------------------------------------------------------------------
# MongoDB Configuration (Docker Deployment)
# -----------------------------------------------------------------------------
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=changeme123
MONGODB_URI=mongodb://admin:changeme123@mongodb:27017
MONGODB_DATABASE=nexora001

# -----------------------------------------------------------------------------
# Vector Search - Qdrant
# -----------------------------------------------------------------------------
USE_QDRANT=true
QDRANT_URL=http://qdrant:6333
# QDRANT_API_KEY=  # Optional, only for Qdrant Cloud

# -----------------------------------------------------------------------------
# Google AI / Gemini
# -----------------------------------------------------------------------------
GOOGLE_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# Generate a secure key: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-change-in-production

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
DEBUG=false
LOG_LEVEL=INFO
ENVIRONMENT=production

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Crawling Configuration
CRAWL_DELAY=1.0
MAX_CRAWL_DEPTH=2
USER_AGENT=Nexora001-Bot/1.0
RESPECT_ROBOTS_TXT=true

# -----------------------------------------------------------------------------
# SMTP Email Configuration
# -----------------------------------------------------------------------------
# Enable/disable email notifications
SMTP_ENABLED=false

# Gmail Example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
SENDER_EMAIL=your-email@gmail.com

# For Gmail, create an App Password at:
# https://myaccount.google.com/apppasswords
# (Enable 2-factor authentication first)

# Other providers:
# - Outlook: smtp-mail.outlook.com:587
# - Yahoo: smtp.mail.yahoo.com:587
# - SendGrid: smtp.sendgrid.net:587

# -----------------------------------------------------------------------------
# DEPLOYMENT NOTES
# -----------------------------------------------------------------------------
# For Docker Deployment (both local and server):
#   - Keep MONGODB_URI using "mongodb" (container name)
#   - Keep QDRANT_URL using "qdrant" (container name)
#   - These are Docker network names, NOT public URLs
#   - Frontend/Widget automatically detect the public URL
#
# For Local Docker Development:
#   - MONGO_ROOT_PASSWORD: Use simple password (changeme123)
#   - JWT_SECRET_KEY: Use simple key (dev-secret-key)
#   - ENVIRONMENT: development
#   - DEBUG: true
#
# For Production Server Deployment:
#   - MONGO_ROOT_PASSWORD: MUST use strong password (openssl rand -base64 32)
#   - JWT_SECRET_KEY: MUST use strong secret (openssl rand -hex 32)
#   - GOOGLE_API_KEY: MUST use real API key
#   - ENVIRONMENT: production
#   - DEBUG: false
#   - Update .env on server BEFORE running ./deploy.sh
#
# Public URL Configuration:
#   - NO configuration needed! System auto-detects:
#     * localhost → http://localhost
#     * Server IP → http://46.250.244.245
#     * Domain → https://yourdomain.com
#   - Widget embed code automatically shows correct URL
#   - Frontend API calls automatically use correct URL
#
# Security:
#   - Never commit .env to git
#   - chmod 600 .env on server
#   - Use firewall: ufw allow 80/tcp && ufw allow 443/tcp

